---
description: Author a DAG-aware execution plan from a spec
---

Invoke the `writing-dag-plans` skill on the following spec/topic: $ARGUMENTS

If `$ARGUMENTS` is empty, ask the user which spec to plan from. If the spec exists as a file, read it. Otherwise treat $ARGUMENTS as the topic to plan around.
