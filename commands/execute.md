---
description: Execute a DAG plan with continuous parallel subagent dispatch
---

Invoke the `executing-dag-plans` skill on the plan file: $ARGUMENTS

If `$ARGUMENTS` is empty, ask the user which plan file to execute. The plan file must conform to the DAG plan format (see `skills/writing-dag-plans/plan-format.md`).
